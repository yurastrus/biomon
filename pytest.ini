[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow
    integration: marks tests requiring real DB
    smoke: marks smoke tests (quick happy-path checks)
filterwarnings =
    ignore::DeprecationWarning

# Coverage gate.
#
# Default invocation:
#   venv/Scripts/python -m pytest --cov=app/camera_traps --cov=app/pam \
#                                 --cov-fail-under=30 --cov-report=term
#
# Baseline (2026-05-20): 35% TOTAL. The 30% threshold guards against
# regressions; raise it gradually (50% → 60%) as more tests are added.
